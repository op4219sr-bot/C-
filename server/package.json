{
  "name": "lightc-license-server",
  "version": "1.0.0",
  "description": "LightC 卡密授权服务（自建后台）",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "gen": "node scripts/gen-cards.js",
    "migrate": "node src/db.js migrate"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "@fastify/static": "^7.0.4",
    "better-sqlite3": "^11.6.0",
    "dotenv": "^16.4.5",
    "fastify": "^4.28.1"
  }
}
